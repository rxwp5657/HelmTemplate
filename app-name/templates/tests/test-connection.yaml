apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app-name.fullname" . }}-test-connection"
  labels:
{{ include "app-name.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "app-name.fullname" . }}:{{ .Values.serverService.port }}']
  restartPolicy: Never
